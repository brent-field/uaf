# Code Examples

Here is some inline code: `print("hello")`.

A fenced code block:

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

Another block with no language:

```
plain text block
second line
```
